"""The spec: arbitration cells, scene geometry, and instruction derivation.

`cells.py` is the single source of truth and must stay stdlib-only -- the gate, the collector,
the renderers and both mutually-unimportable venvs all read it.
"""
